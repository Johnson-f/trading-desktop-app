[package]
name = "zaned-api-client"
version = "0.1.0"
edition = "2024"

[dependencies]
zaned-core = { path = "../core" }
reqwest = { version = "0.12", features = ["json"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
