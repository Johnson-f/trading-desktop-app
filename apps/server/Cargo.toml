[package]
name = "zaned-server"
version = "0.1.0"
edition = "2024"

[dependencies]
zaned-core = { path = "../../crates/core" }
axum = "0.8"
tokio = { version = "1.50", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
