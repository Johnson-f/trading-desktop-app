# Copy to apps/server/.env (gitignored) and fill in real values.
# `cargo run -p zaned-server` will auto-load this via dotenvy.

REDIS_URL=redis://:changeme@vmi3204337.contaboserver.net:6379

# Required: get from Clerk dashboard → API Keys → Secret Keys
CLERK_SECRET_KEY=sk_test_changeme

# Optional
# MAX_WARM_SYMBOLS=50
# TICK_SERVICE_WS_BIND=0.0.0.0:8765
# TODAY_BARS_TTL_SECS=129600
# UPDATES_STREAM_MAXLEN=200
# COLD_SUBSCRIPTION_TTL_SECS=300
# RUST_LOG=zaned_server=info,markets=warn
