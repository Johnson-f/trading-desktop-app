# Copy to apps/symbol-service/.env (gitignored) and fill in real values.
# `cargo run -p symbol-service` will auto-load this via dotenvy.

TYPESENSE_URL=http://vmi3204337.contaboserver.net:8108
TYPESENSE_API_KEY=changeme
REDIS_URL=redis://:changeme@vmi3204337.contaboserver.net:6379

# Optional overrides
# SYMBOL_SERVICE_SCHEDULE_HOUR_UTC=2
# SYMBOL_SERVICE_COLLECTION=tickers
# RUST_LOG=symbol_service=info,markets=warn
