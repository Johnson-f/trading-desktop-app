# Copy to apps/tick-service/.env (gitignored) and fill in real values.
# `cargo run -p tick-service` will auto-load this via dotenvy.

REDIS_URL=redis://:changeme@vmi3204337.contaboserver.net:6379

# Optional
# MAX_WARM_SYMBOLS=50
# TICK_SERVICE_WS_BIND=0.0.0.0:8765
# TODAY_BARS_TTL_SECS=129600
# UPDATES_STREAM_MAXLEN=200
# COLD_SUBSCRIPTION_TTL_SECS=300
# RUST_LOG=tick_service=info,markets=warn
