# Copy to apps/desktop/.env (gitignored) and fill in real values.
# Get these from your Clerk dashboard → OAuth Applications.
#
# The OAuth Application MUST have:
#   - Public client (PKCE)               : enabled
#   - Redirect URIs                       : http://127.0.0.1:*/callback
#   - Scopes                              : openid profile email

CLERK_ISSUER=https://your-instance.clerk.accounts.dev
CLERK_CLIENT_ID=pk_test_changeme
